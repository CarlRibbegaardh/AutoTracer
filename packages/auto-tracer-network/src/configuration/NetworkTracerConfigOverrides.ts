import type { NetworkTracerConfig } from "./NetworkTracerConfig.js";

/**
 * Defines project or persisted values that override NetworkTracer package defaults.
 */
export type NetworkTracerConfigOverrides = Partial<NetworkTracerConfig>;
